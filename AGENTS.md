# Agent 작업 규칙

## 구현 원칙

1. 요구를 온전히 충족하는 가장 단순한 구현을 택하고 추측성 추상화·설정·호환 계층을 만들지 않는다.
2. 작은 end-to-end 단위를 먼저 검증하고 확인된 결과 위에 기능을 더한다.
3. 요청 범위만 변경하고 관련 없는 사용자 작업과 기존 변경을 보존한다.
4. 프로젝트와 사용자 응답, 문서는 한국어를 기본으로 하되 코드 식별자·명령·검색어는 영어를 사용할 수 있다.
5. 유지보수되는 검증된 라이브러리가 복잡도를 낮추면 직접 구현하기 전에 실제 제공 기능·타입·라이선스를 확인한다.

## 아키텍처 경계

- Electron main process는 provider CLI, 네트워크와 로컬 파일을 수집한다. renderer는 정규화된 `ProviderSnapshot`만 받고 자격증·파일시스템·provider 원본 출력에 접근하지 않는다.
- provider별 CLI/API 파서를 서로 격리하고 공통 snapshot 계약 뒤에 둔다. provider 하나의 실패가 다른 provider를 중단시키지 않도록 한다.
- quota는 벤더가 제공한 계정 전체 값이고 토큰은 이 PC의 로컬 로그 집계다. 한쪽으로 다른 쪽을 추정·역산하지 않는다.
- 벤더가 제공하지 않은 quota는 0%·무제한·reset 완료로 표시하지 않고 미제공 상태로 유지한다.
- 실패 시 마지막 정상값을 stale로 남기고 오류와 신선도를 숨기지 않는다.

## 인증과 민감정보

- Codex·Claude Code·Antigravity의 인증 파일, OS 키체인과 CLI 세션을 수정·갱신·교체·삭제하지 않는다. 로그인과 token refresh는 해당 벤더 CLI에만 맡긴다.
- Claude Code 구독 OAuth token과 credential은 opt-in 여부와 관계없이 직접 읽거나 비공개 usage endpoint에 중계하지 않는다. 지원 범위는 [Anthropic 인증 경계 결정](docs/decisions/0001-anthropic-credential-boundary.md)을 따른다.
- 앱이 실행한 벤더 CLI는 자신의 소유권에 따라 인증 상태를 갱신할 수 있다. 따라서 CLI 실행 전후 인증 파일 동일성을 전체 앱의 불변 조건으로 두지 않고, 앱 코드의 직접 read/write 부재를 검증한다.
- 자격증 직접 읽기는 제품 문서에 기록된 사용자 opt-in 읽기 전용 fallback에서만 허용한다. 새 경로를 추가하려면 먼저 명시적 승인과 위협 검토를 받는다.
- access token, refresh token, API key, cookie, `Authorization` 헤더, credential 본문을 출력·복사·로그·통계·fixture·스냅샷·문서·오류 메시지에 남기지 않는다.
- 실제 인증정보나 개인정보를 저장소에 반입하지 않고 테스트에는 허구 데이터만 사용한다.
- 민감값이 의심되면 값을 출력하지 말고 파일, 위치와 탐지 규칙만 보고한다.

## 검증 규칙

- 변경한 provider는 정상 quota, quota 미제공, CLI 미설치, 인증 만료, 429, 네트워크 실패, 파서 불일치 중 해당하는 성공·대표 실패 경로를 검증한다.
- 인증 파일을 읽는 opt-in 직접 읽기 경로를 변경하면 해당 코드만 격리해 실행 전후 파일 내용과 변경 시각이 같음을 검증한다. 검증 결과에 내용은 출력하지 않는다.
- 대용량 JSONL은 전체 문자열 로드 대신 청크 스트리밍으로 테스트하고 멀티바이트 경계와 증분 추가를 포함한다.
- 자동 검사가 통과해도 커밋 전에 staged diff 전체와 민감정보 검사 결과를 직접 읽는다.

## 선택 참조

현재 작업과 일치할 때만 [Agent 참조 인덱스](docs/agent/INDEX.md)의 해당 문서를 읽는다. 파일 탐색으로 알 수 있는 세부 구조나 일회성 요구를 이 파일에 추가하지 않는다.
